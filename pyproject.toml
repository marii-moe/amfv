[project]
name = "amfv"
version = "0.0.1"
description = "Shared workspace for the AMFV runtime components"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "amfv-datasets",
    "amfv-decomposer",
    "amfv-eval",
    "amfv-search",
    "amfv-utils",
    "amfv-verifier",
    "jinja2>=3.1.6",
    "pytest>=8.0",
    "ruff>=0.11",
]

[tool.uv]
package = false
environments = ["sys_platform == 'linux'"]

[tool.uv.sources]
amfv-datasets = { workspace = true }
amfv-decomposer = { workspace = true }
amfv-eval = { workspace = true }
amfv-search = { workspace = true }
amfv-utils = { workspace = true }
amfv-verifier = { workspace = true }
torch = { index = "pytorch-cu128" }

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.workspace]
members = [
    "datasets",
    "decomposer",
    "decomposer/eval",
    "search",
    "utils",
    "verifier",
]
exclude = [
    "baseline",
    "training",
]

[tool.pytest.ini_options]
testpaths = [
    "datasets/test",
    "decomposer/test",
    "decomposer/eval/test",
    "search/test",
    "utils/test",
    "verifier/test",
]
